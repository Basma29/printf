#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
char c = 'c';
char name[] = "Abdelrahman";

_printf("Hello, %s. your name is printed using %% and the %c character", name, c);
return (0);
}
